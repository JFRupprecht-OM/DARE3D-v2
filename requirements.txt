# --------- deep learning core (handled separately for CUDA) ---------
# NOTE: torch / torchvision / torchaudio are installed separately with the right
# CUDA build (see install.sh / install.ps1 / README) — not listed here so we never
# overwrite a user's CUDA wheel with a default PyPI build.

# --------- lightning / metrics ---------
lightning>=2.0.0
torchmetrics>=0.11.4

# --------- hydra ---------
hydra-core>=1.3,<1.4
hydra-colorlog>=1.2
hydra-optuna-sweeper>=1.2

# --------- loggers ---------
mlflow
tensorboard

# --------- config / CLI / utils ---------
rootutils
rich
click
omegaconf

# --------- dl library -----
monai>=1.3,<1.4
augmend

# ------ image / numeric ---
numpy>=1.23,<1.27
scipy
numba>=0.59,<0.61
scikit-image
tifffile
matplotlib
tqdm

# --------- napari plugin (GUI) ---------
napari
magicgui

# --------- dev tools ---------
pre-commit
pytest

# optional: if you need Qt backend control for napari, do not pin PyQt here;
# use conda install -c conda-forge pyqt (check in Readme for details)
